# Single 2D object detection (bounding box)
string class_name           # e.g. "apple", "cup", "bottle"
float32 confidence          # 0.0 to 1.0

# Bounding box in pixels (top-left corner + width/height)
int32 x
int32 y
int32 width
int32 height
